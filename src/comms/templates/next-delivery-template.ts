export function generateNextDelivery(
  firstName: string,
  catNames: string,
): string {
  return `Welcome to KatKin, ${firstName}! We're super excited for ${catNames} to join the KatKin club and start loving fresh!`;
}
